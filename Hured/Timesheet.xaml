<controls:MetroWindow x:Class="Hured.Timesheet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        
        xmlns:local="clr-namespace:Hured"
        mc:Ignorable="d"
        Title="Табель посещаемости" >
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition ></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0">Место работы</Label>
            <TreeView x:Name="tvUnits" Grid.Column="0" Grid.Row="1" Grid.RowSpan="3"
                      SelectedItemChanged="TvUnits_OnSelectedItemChanged">
            </TreeView>
            
            <GridSplitter Grid.Row="0" Grid.RowSpan="3" Grid.Column="1"
                          ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" ShowsPreview="True"
                      VerticalAlignment="Stretch" Width="5"></GridSplitter>

            <DataGrid Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Name="dgTimeSheet"
                      BeginningEdit="DgTimeSheet_OnBeginningEdit" Margin="3">
            </DataGrid>

            <Button x:Name="bClose" Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right" IsDefault="True" Click="bClose_Click">Закрыть</Button>
        </Grid>
    </Border>
</controls:MetroWindow>
